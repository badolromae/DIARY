# 다이어리 v1.2 업데이트 안내

## 1. 위젯이 안 됐던 진짜 원인

**안드로이드 위젯에서는 기본 `View` 태그를 쓸 수 없습니다.**

위젯 화면(RemoteViews)은 일반 앱 화면과 달리 **허용된 요소만** 넣을 수 있습니다.
`LinearLayout`, `FrameLayout`, `TextView`, `ImageView`, `ImageButton`, `ProgressBar`,
`ListView`, `GridView` 정도이고, 가장 기본인 `View` 태그는 **목록에 없습니다.**

제가 구분선과 점 표시에 `View` 를 썼는데, 위젯은 그 요소를 만나면 그 레이아웃 전체를
그리지 못하고 포기합니다. 그래서:

| `View` 가 있던 파일 | 화면에 보였던 증상 |
|---|---|
| `widget_month.xml` (구분선) | **"위젯을 추가할 수 없습니다"** — 월 위젯 전체 실패 |
| `widget_month_cell.xml` (기록 점) | 월 달력 칸이 안 그려짐 |
| `widget_week_header.xml` (초록 막대) | 날짜 헤더 대신 **"로드 중.."** |

`View` 가 없던 일 위젯만 정상으로 보였던 것도 이걸로 설명됩니다.
이번에 3곳 모두 허용된 요소로 바꿨고, 위젯 레이아웃 전체를 검사해서
허용된 요소만 남은 것을 확인했습니다.

---

## 2. 이번에 바뀐 것

### ① 월 위젯
- **정상 추가·표시됩니다.**
- **이번 달 날짜만** 보여줍니다. 지난달·다음달 칸은 비어 있습니다.
- **칸을 누르면 그 날짜로 앱이 열립니다.**
- 한 칸에 `날짜 / (기록표시)음력 / 공휴일` 3줄. 오늘은 초록 테두리로 표시됩니다.
- 공휴일·일요일은 빨간색, 토요일은 파란색입니다.

### ② 주 위젯
- 헤더가 **`8/24 월요일`** 형식으로 바뀌었습니다. (오른쪽에 음력·공휴일)
- `기록 없음 (+ 추가)` 문구를 없애고 **빈 줄**로 두었습니다.
- **`로드 중..` 표시가 사라졌습니다.** (헤더 실패가 원인이었고, 추가로 빈 줄을
  로딩 화면으로 지정해 어떤 경우에도 안 뜨게 했습니다.)

### ③ 일 위젯
- **0시부터 23시까지 1시간 간격**으로 주 위젯과 같은 모양으로 표시됩니다.
- **위아래로 드래그하면 24시간 전체**를 볼 수 있습니다.
- 지금 시각의 시간대에는 `지금` 표시가 붙습니다.
- **시간대를 누르면 그 시각으로 새 일기가 바로 열립니다.** (예: 오후 3시를 누르면
  시작 시각이 오후 3시로 채워진 편집 화면)
- 종일 일정과 여러 날에 걸친 일정은 맨 위 `종일` 칸에 모아 보여줍니다.

### ④ 앱 화면
- **설정 톱니바퀴·검색 아이콘이 흰색**으로 바뀌었습니다.
- **날짜마다 흰색 테두리**로 칸이 구분됩니다.
- 날짜 글씨를 키우고(16sp) 음력·기념일도 키웠습니다. 칸 높이도 늘렸습니다.
- 오늘 표시 테두리가 어두운 모드에서 안 보였던 문제를 고쳤습니다. (검정 → 초록)
- 긴 이름을 줄였습니다: `대체공휴일(광복절)` → **`대체휴일`**,
  `설날 연휴` → `설연휴`, `부처님오신날` → `석가탄신`

---

## 3. 깃허브에 올리는 방법

### 새로 만들어야 하는 파일 3개

**드래그하지 마시고** `Add file ▸ Create new file` 로 아래 경로를 **직접 입력**해서
만들어 주세요. (드래그는 폴더 경로를 잃어버립니다)

```
app/src/main/res/layout/widget_blank_row.xml
app/src/main/res/drawable/bg_widget_today_cell.xml
app/src/main/res/drawable/cal_cell_border.xml
```

내용은 압축을 푼 폴더의 같은 경로에 있는 파일에서 복사하세요.

### 내용이 바뀐 파일 17개

`app` 폴더와 `.github` 폴더를 **폴더째로** 드래그해서 덮어쓰시면 됩니다.
(이미 있는 파일은 새 내용으로 바뀝니다)

바뀐 목록:
```
.github/workflows/build.yml          ← 서명키 복원 단계 (이미 넣으셨으면 동일)
app/build.gradle                     ← 버전 1.2
app/src/main/java/.../ui/EntryEditorActivity.kt
app/src/main/java/.../ui/MainActivity.kt
app/src/main/java/.../ui/MonthCalendarView.kt
app/src/main/java/.../util/DateUtil.kt
app/src/main/java/.../util/KoreanHolidays.kt
app/src/main/java/.../widget/DayWidgetService.kt
app/src/main/java/.../widget/MonthWidgetProvider.kt
app/src/main/java/.../widget/WeekWidgetService.kt
app/src/main/res/drawable/bg_day_today.xml
app/src/main/res/layout/widget_month.xml
app/src/main/res/layout/widget_month_cell.xml
app/src/main/res/layout/widget_week_header.xml
app/src/main/res/menu/menu_main.xml
app/src/main/res/values/colors.xml
app/src/main/res/values-night/colors.xml
```

> `debug-keystore.b64` 도 zip에 들어 있습니다. 이미 저장소에 넣으셨으면 같은 내용이라
> 그대로 두시면 되고, 아직 안 넣으셨다면 이번에 꼭 넣어주세요. 그래야 앞으로
> 앱을 지우지 않고 업데이트 설치가 됩니다.

---

## 4. 설치 후 꼭 해주세요

**홈 화면의 기존 위젯 3개를 모두 지우고 다시 추가**해 주세요.

지금 홈 화면에 있는 위젯은 실패한 상태로 붙어 있어서, 앱만 업데이트해도
그 자리에 그대로 남아 있을 수 있습니다.

1. 기존 다이어리 위젯 길게 누르기 ▸ **제거**
2. 홈 화면 빈 곳 길게 누르기 ▸ **위젯** ▸ **다이어리**
3. 월 / 주 / 일 달력 다시 추가
4. 길게 눌러 핸들을 끌어 **화면 가득** 크기로 조절

---

## 5. 확인해보실 것

- 월 위젯이 **추가되는지**, 날짜를 누르면 **그 날짜로 앱이 열리는지**
- 주 위젯 헤더가 `8/24 월요일` 로 나오는지, `로드 중..` 이 사라졌는지
- 일 위젯을 드래그해서 **0시~23시**가 다 보이는지
- 앱의 톱니바퀴가 **흰색**인지, 날짜 칸에 **흰 테두리**가 보이는지
